import { Button } from '@/components/ui/button';
import { Alert, AlertDescription } from '@/components/ui/alert';
import { Alert, AlertDescription } from '@/components/ui/alert';
  return (

          <Envelope className="mr-2 h
  return (
      <Dialo
          <DialogTitle>Email 
        <Alert>
            Email digest system is temporarily disabled. This
        </Alert>
        </Button>
}


          <DialogTitle>Email Digest System</DialogTitle>
        </DialogHeader>





      </DialogContent>
    </Dialog>

}
